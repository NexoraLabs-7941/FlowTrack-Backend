
spring.profiles.active=dev
#Spring Boot Application Name
spring.application.name=flowtrack-backend

#Spring Boot Application Configuration
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:flowtrack}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#Spring Boot JPA Configuration
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#Spring Data JPA Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.connection.autocommit=false
spring.jpa.hibernate.naming.physical-strategy=com.nexoralabs.flowtrack.shared.infrastructure.persistence.jpa.configuration.strategy.SnakeCaseWithPluralizedTablePhysicalNamingStrategy

#OpenAPI Documentation Configuration
documentation.application.description=Flowtrack Backend API - Sistema de gestion de inventario
documentation.application.version=1.0.0


# JWT Configuration Properties-

# Custom Properties for JWT
# suppress inspection "SpringBootApplicationProperties"
authorization.jwt.secret = ${JWT_SECRET:WriteHereYourSecretStringForTokenSigningCredentials}
# suppress inspection "SpringBootApplicationProperties"
authorization.jwt.expiration.days = ${JWT_EXP_DAYS:7}

server.port=${PORT:8081}

# Edge Vision (Python) - URLs consumidas por el orquestador Java
# Edge Vision (Python) - URLs consumidas por el orquestador Java
# Edge Vision (Python) - URLs fijas para desarrollo local
# Desarrollo local: Python corre en el mismo host (evita la página intersticial de ngrok).

edge.vision.start-url= https://shaded-nativity-travel.ngrok-free.dev/api/v1/vision/start
edge.vision.stop-url= https://shaded-nativity-travel.ngrok-free.dev/api/v1/vision/stop
edge.vision.detect-image-url=http://localhost:8000/api/v1/vision/detect-image

# JWT (lee de env; usa valor por defecto solo en desarrollo)
# authorization.jwt.secret=${JWT_SECRET:WriteHereYourSecretStringForTokenSigningCredentials}
# authorization.jwt.expiration.days=${JWT_EXP_DAYS:7}
#edge.vision.start-url=http://localhost:8000/api/v1/vision/start
#edge.vision.stop-url=http://localhost:8000/api/v1/vision/stop

# KAFKA CONFIGURATION
spring.kafka.consumer.bootstrap-servers=pkc-56d1g.eastus.azure.confluent.cloud:9092
spring.kafka.consumer.group-id=flowtrack-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer

# Los listeners parsean JSON manualmente para soportar varios contratos de evento en distintos topics.
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer

# Seguridad para conectar a Confluent Cloud
spring.kafka.properties.security.protocol=SASL_SSL
spring.kafka.properties.sasl.mechanism=PLAIN
spring.kafka.properties.sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required username="${KAFKA_USERNAME:FYTHUU7K3L2N43XK}" password="${KAFKA_PASSWORD:cflthuqy3igT8XQyZvyhpmuUO49okhvUbdbqorZQB9NN4gCtR0oPAHYKf+ClOj7w}";

# External Service Integrations
integration.google-sheets.api-url=${GOOGLE_SHEETS_API_URL:https://sheets.googleapis.com/v4/spreadsheets}
integration.telegram.api-url=${TELEGRAM_API_URL:https://api.telegram.org/bot}
integration.telegram.bot-token=${TELEGRAM_BOT_TOKEN:mock_bot_token}
integration.firebase.api-url=${FIREBASE_API_URL:https://fcm.googleapis.com/fcm/send}
app.firebase.config-path=${FIREBASE_CONFIG_PATH:classpath:firebase-service-account.json}

# Google Sheets Default Spreadsheet ID
app.google-sheets.default-spreadsheet-id=1kx4PEMlhGp8Hn_CRsci92-vsQbYhGMK_68-7GMl0JsA


